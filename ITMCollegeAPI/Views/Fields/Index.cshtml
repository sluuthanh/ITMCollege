@model IEnumerable<ITMCollegeAPI.Models.Field>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FieldName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Stream)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FieldName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Stream.StreamId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FieldId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FieldId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FieldId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
